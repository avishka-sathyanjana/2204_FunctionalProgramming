object question123 extends App{
    
    //declaring variables

    var k, i, j = 2;
    var m, n = 5;

    var f = 12.0f;
    var g = 4.0f;

    var c = 'X';

    //evaluating and displaying expressions
    println("a) Result = " + (k + 12 * m));
    println("b) Result = " + (m / j));
    println("c) Result = " + (n % j));
    println("d) Result = " + (m / j * j));
    println("e) Result = " + (f + 10 * 5 + g));

    i += 1;

    println("f) Result = " + (i * n));

}